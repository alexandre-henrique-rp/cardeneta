version: "3.8"

services:
  api:
    image: api_cardeneta
    build: .
    restart: always
    volumes:
      - ./db/:/app/db
      - ./files/:/app/files
    environment:
      - .env
    networks:
      - cardeneta

networks:
  cardeneta:
    external: true
    name: cardeneta
     