version: "3.8"

services:
  backend:
    image: api_cardeneta
    build: ./backend
    ports:
      - "3030:3030"
    restart: always
    volumes:
      - ./db/:/app/db
      - ./files/:/app/files
    networks:
      - cardeneta

  # frontend:
  #   image: cardeneta_web
  #   build: ./frontend
  #   restart: always
  #   ports:
  #     - "3000:3000"
  #   networks:
  #     - cardeneta
  #   depends_on:
  #     - backend

networks:
  cardeneta:
    external: true
    name: cardeneta
